import React from 'react'
import { Element } from 'react-scroll';

type Props = {}

const DevWork = (props: Props) => {
  return (
    <Element name="devWork" className='h-screen bg-cyan-100 border-solid border-violet-500 hover:bg-neutral-700 border-8'>DevWork</Element>
  )
}

export default DevWork;